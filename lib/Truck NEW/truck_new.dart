

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class truck_new extends StatelessWidget {
  const truck_new({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text("Truck New"),
      ),
    );
  }
}
